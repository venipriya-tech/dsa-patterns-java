public static int[] arrayProductExceptItself(int[] arr) {

    int leftProduct = 1;
    int[] prodArray = new int[arr.length];

    for (int i = 0; i < arr.length; i++) {
        prodArray[i] = leftProduct;
        leftProduct *= arr[i];
    }

    int rightProduct = 1;

    for (int i = arr.length - 1; i >= 0; i--) {
        prodArray[i] = prodArray[i] * rightProduct;
        rightProduct *= arr[i];
    }

    return prodArray;
}
